# Copy this file to `.env.local` (recommended) and fill in values.
# Never commit real keys.

# AI provider keys used by the local AI service (server/ai-service.mjs)
GEMINI_API_KEY=

# Optional: which provider/model the server should use by default
AI_PROVIDER=google
AI_MODEL=gemini-flash-lite-latest

# Production only: Comma-separated list of allowed origins for CORS
# ALLOWED_ORIGINS=https://yourdomain.com
